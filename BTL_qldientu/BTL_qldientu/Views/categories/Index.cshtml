@model IEnumerable<BTL_qldientu.Models.category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cat_username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cat_username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cat_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cat_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cat_id })
        </td>
    </tr>
}

</table>
